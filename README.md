# Github test

This is a test of github so far today I have done the following

```
  348  git status
  349  git config
  350  git config --global --list
  351  git config --global user.name "renatsb"
  352  git config --global user.email rbikmajevs@gmail.com
  353  git config --global --list
  358  git init
  359  ls -al .git
  360  git status
  361  touch .gitignore
  362  gedit .gitignore
  363  git add .
  364  git status
  366  git add .
  367  git status
  368  git commit -am "Initial commit of project"
  371  git commit -am "added a comment"
  372  git diff
  373  git status
  374  git log
  378  ssh -t git@github.com
  379  git remote add origin git@github.com:RenatsB/PPPLab1.git
  380  git push -u origin master
  383  history git
  384  history .git
  385  history | grep git
```